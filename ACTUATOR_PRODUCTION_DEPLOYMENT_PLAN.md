# 本地执行器生产与内网部署开发方案

## 目标

平台用户下载并启动本地执行器后，可以在平台点击人工采集元素地图，由平台安全、稳定地连接已在线执行器，并触发执行器在用户本机桌面打开浏览器采集窗口。

## 架构边界

- 平台后端是调度中枢，负责用户鉴权、执行器登记、任务创建、任务下发、结果回收和审计。
- 本地执行器只主动出站连接平台后端，不要求用户机器开放入站端口。
- 人工采集窗口由本地执行器打开，必须运行在用户本机桌面会话中，`headless=False`。
- 浏览器插件不是采集引擎，只作为可选增强，用于登录态、当前页上下文或一键授权辅助。

## 生产安全要求

- HTTP 前端请求受 `DJANGO_ALLOWED_HOSTS`、`DJANGO_CORS_ALLOWED_ORIGINS`、`DJANGO_CSRF_TRUSTED_ORIGINS` 控制。
- WebSocket 走 WSS，后端保留 `AllowedHostsOriginValidator` 来源校验。
- 执行器连接不得只信任 `actuator_id`，必须增加注册令牌。
- 任务下发携带 `dispatch_id`，执行器按 `task_id + dispatch_id` 或 `element_map_id + dispatch_id` 做幂等。
- 后端只允许向已认证、在线、未过期的执行器下发采集任务。
- 执行器连接、任务下发、任务完成、任务失败需要保留审计字段。

## 内网部署要求

- 内网模式与生产公网模式共用同一协议，只通过配置切换后端地址和证书策略。
- 执行器只需要访问后端 HTTPS/WSS 地址。
- 后端反向代理需支持 WebSocket Upgrade、长连接超时、请求体大小和 TLS 证书配置。

## 平台接入要求

- 平台提供执行器下载页，展示 Windows/Linux/macOS 下载入口、校验值与发布说明。
- 平台提供安装/接入指引，明确 `api_url`、`ws_url`、注册令牌、心跳间隔和 `headless=false` 要求。
- 平台提供可复制的执行器配置模板，降低用户首次接入成本。
- 平台执行器管理页继续展示在线状态、版本、最近心跳与认证状态。

## 稳定性要求

- 执行器 WebSocket 断线后自动重连。
- 执行器定时上报心跳，后端维护 `last_seen_at` 与在线 TTL。
- 平台展示执行器在线、离线、过期、版本、浏览器类型、headless 状态。
- 已分配但 WebSocket 丢失的任务通过执行器轮询补偿拉取。
- 活跃任务通过心跳续租，避免被重复下发。
- 人工采集浏览器启动时最大化窗口，并同步 viewport。

## 开发任务

1. 执行器配置增加注册令牌字段，连接 WebSocket 时携带令牌。
2. 后端 WebSocket 执行器连接校验注册令牌，令牌缺失或错误时拒绝连接。
3. 后端保存执行器在线信息：`actuator_id`、IP、版本、浏览器、`last_seen_at`、认证状态。
4. 执行器定时发送心跳，后端更新在线状态。
5. 人工采集下发时只选择已认证且心跳未过期的执行器。
6. 执行器收到重复 `dispatch_id` 时幂等跳过或返回已有结果。
7. 保留现有 `pending_for_actuator` 补偿拉取机制，并确保活跃任务续租。
8. 增加单元测试覆盖连接鉴权、心跳刷新、人工采集下发只使用认证执行器。
9. 在平台侧增加执行器下载页与安装/接入指引页签，提供平台配置模板与下载入口。

## 验收标准

- 未配置注册令牌的生产后端拒绝执行器连接。
- 配置正确令牌的执行器可以在线登记并显示状态。
- 平台点击人工采集后，后端只向认证在线执行器下发任务。
- 执行器断线重连后能恢复在线状态。
- WebSocket 丢消息时，执行器补偿轮询可重新拉取已分配任务。
- 人工采集浏览器在用户本机桌面以非 headless 最大化方式打开。
- 平台可以直接展示执行器下载入口和接入模板，用户无需离开平台查文档。
- 开发完成后对照本文件逐项确认无遗漏。
