# 执行器 Vibe Coding 开发计划

## 目标

把本地执行器从“源码可运行”推进到“可下载、可安装、可接入、可回归”的完整交付链路。

用户在平台下载执行器后，可以按指引在 Windows / Linux 本机完成启动；平台点击人工采集时，能够稳定连接到已在线执行器，并在用户桌面打开可交互的浏览器采集窗口。

## 当前基线

- 平台已具备执行器下载页和接入页的接口基础。
- Windows 下载链路已支持优先返回打包版，缺失时回退源码包。
- 执行器、后端、前端都已开始围绕“在线执行器 + 人工采集 + 回归测试”做联动改造。
- 当前需要继续收敛的是：
  - Windows 产物打包与发布说明。
  - 平台下载页的默认下载体验。
  - 执行器安装、启动、连接、重连的完整步骤。
  - 人工采集浏览器窗口的显示与全屏可用性。
  - 端到端回归与失败定位机制。

## 开发原则

1. 优先保留现有架构，不引入新框架。
2. 平台负责分发、登记、调度和审计。
3. 本地执行器只做主动出站连接，不要求用户开放入站端口。
4. 下载页、安装指引、配置模板和在线状态要放在同一条用户路径上。
5. 每次改动都要有可回归的验收点，不做只改代码不验收的提交。

## 交付拆分

### 1. Windows 打包产物

- 明确 Windows 产物目录结构。
- 打包后默认包含：
  - `WHartTest_Actuator.exe`
  - `config.toml`
  - `start.bat`
  - 必要运行时资源
- 平台下载逻辑优先发放打包版，不再强依赖源码包。
- 下载页展示“打包版 / 源码版 / 校验信息 / 启动说明”。

### 2. 平台下载页

- 下载页提供执行器版本、平台、校验值和发布说明。
- 接入页提供：
  - 后端地址
  - WebSocket 地址
  - 注册令牌
  - 心跳间隔
  - 启动方式
  - 常见故障说明
- 平台展示在线执行器列表，只保留最新可用实例作为默认目标。

### 3. 执行器接入与启动

- 用户下载后先解压，再按平台给出的配置模板生成 `config.toml`。
- Windows 场景默认通过 `start.bat` 启动。
- 启动后执行器自动注册、建立 WebSocket 连接并上报心跳。
- 异常断线后自动重连，不要求用户手工刷新页面。

### 4. 人工采集窗口

- 采集浏览器窗口必须在用户桌面会话内打开。
- 启动后尽量最大化，必要时同步 viewport。
- 采集窗口不能被默认尺寸限制成不可操作的小弹窗。
- 手动登录、隐藏输入框、全屏展开等场景要作为回归重点。

### 5. 生产与内网接入

- 生产环境与内网环境使用同一套协议与配置模型。
- 不把 CORS、前端访问和执行器连接混在一起处理。
- 执行器仅访问后端 HTTPS / WSS 地址。
- 内网主机访问平台前端时，单独处理端口暴露、反代和访问地址。

## 实施阶段

### 阶段一：交付可下载包

1. 确认 Windows 打包脚本和产物目录。
2. 确认平台下载接口优先返回打包版。
3. 把下载页文案改成“下载包 + 启动步骤 + 接入模板”。
4. 为打包版补最小启动验证。

### 阶段二：接入链路打通

1. 执行器启动后能稳定连上平台。
2. 平台能看到在线状态、版本、最近心跳。
3. 人工采集任务能下发到最新在线执行器。
4. 断线重连后能恢复可用状态。

### 阶段三：采集窗口可用性

1. 打开浏览器采集窗口时默认最大化。
2. 解决白屏、非全屏、窗口尺寸过小等问题。
3. 补回“手动可输入隐藏登录框”的验证场景。
4. 把窗口状态纳入任务证据。

### 阶段四：回归与验收

1. 下载页回归。
2. 安装与启动回归。
3. 在线连接回归。
4. 人工采集回归。
5. 失败定位与 trace 回归。

## 开发节奏

1. 先确认文件和目录结构，不大范围重构。
2. 每次只推进一个闭环：打包、下载、启动、连接、采集、回归。
3. 每次改动后马上补测试或手工验证。
4. 出现失败时先定位根因，再决定是修复、回滚还是补规则。

## 验收标准

- Windows 用户可以拿到默认推荐的打包产物。
- 用户能按平台说明完成安装和启动。
- 执行器能稳定连接平台并保持在线。
- 平台人工采集能唤起用户桌面的浏览器窗口。
- 采集窗口尺寸满足手工输入和页面交互。
- 失败任务可以通过 trace / 日志快速定位根因。
- 同目录下的开发方案文件能覆盖当前实现和后续回归项。

## 对照文件

- [ACTUATOR_PRODUCTION_DEPLOYMENT_PLAN.md](./ACTUATOR_PRODUCTION_DEPLOYMENT_PLAN.md)
- [current-modifications-and-issues.md](./current-modifications-and-issues.md)
- [AI_UI_EXECUTION_CHAIN_PLAN.md](./AI_UI_EXECUTION_CHAIN_PLAN.md)

